#!/bin/bash -e
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

#case "$1" in
#  -v|--version)
#    version="$2"
#esac
echo "Calling deploy from install"
${DIR}/deploy.sh
echo "Install complete"
